# Drop a `preview.png` (or `.jpg`/`.gif`/`.webp`) here to populate the
# pi.dev gallery card for @jmcombs/pi-tavily-search. Optionally drop
# `preview.mp4` here too for an autoplay-on-hover preview on desktop.
